[server]
    host = localhost
    port = 10000
    devel = false

[renderer]
    dir = tempdir

[dispatcher]
    middleware = Session
    middleware = Static
    middleware = Logger

[i18n]
    dir = langdir
    language = fr
    language = de
