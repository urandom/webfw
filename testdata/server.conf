[server]
    host = localhost
    port = 10000
    devel = false

[renderer]
    dir = tempdir

[dispatcher]
    middleware
    middleware = Session
    middleware = Static
    middleware = Logger

[i18n]
    dir = langdir
    language
    language = fr
    language = de
